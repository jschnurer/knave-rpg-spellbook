import React from 'react';
import { Text } from 'react-native';

const RulesHeader = ({children}) => <Text style={{fontSize: 24, paddingTop: 5, paddingBottom: 5, fontWeight: 'bold'}}>{children}</Text>;
export default RulesHeader;