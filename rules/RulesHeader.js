import React from 'react';
import { Text } from 'react-native';

const RulesHeader = ({children}) => <Text style={{fontSize: 20, paddingTop: 5, paddingBottom: 5, fontWeight: 'bold'}}>{children}</Text>;
export default RulesHeader;