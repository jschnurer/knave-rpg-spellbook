import React from 'react';
import { Text } from 'react-native';

const RulesHeader2 = ({children}) => <Text style={{fontSize: 16, paddingTop: 3, paddingBottom: 3, fontWeight: 'bold'}}>{children}</Text>;
export default RulesHeader2;